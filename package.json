{
  "name": "brasil-pesquisa-eleitoral-tse",
  "version": "4.0.2",
  "private": true,
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cat dashboard-parts/*.txt > public/dashboard.js && node --check public/dashboard.js && node --check polls-data.js && npx @tailwindcss/cli -i ./src/input.css -o ./public/tailwind.css --minify && cp index.html polls-data.js public/"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "iconv-lite": "^0.6.3"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.10",
    "tailwindcss": "^4.1.10"
  }
}
